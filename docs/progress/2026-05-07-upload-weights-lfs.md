# Progress Update - May 7, 2026

## Changes
- Tracked `geoclip/model/weights/*.pth` using Git LFS.
- Removed `geoclip/model/weights/neg_sampling_weights.pth` from `.gitignore`.
- Uploaded model weights to the remote repository.

## Goal
To upload the necessary large model weights into the GitHub repository using Git LFS, so that others can pull and use the model directly without having to download weights manually.
