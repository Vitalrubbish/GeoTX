# Progress Update - May 6, 2026

## Changes
- Ignored large weight file `geoclip/model/weights/neg_sampling_weights.pth` via `.gitignore` to bypass GitHub Fork LFS limitations.
- Updated `README.md` and `docs/dev/test.md`.

## Goal
Successfully push local changes to the `main` branch without exceeding GitHub's file size limit for forks.
