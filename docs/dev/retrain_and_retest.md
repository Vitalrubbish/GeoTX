# Retrain and Retest
Now I have modified the dataset split strategy, added more scripts and so on to make the credibility of my research. Now your task is to retrain the model and retest to get the new result data, which will be used in paper writing.

## TODO LIST
1. Train and test the model to get the performance data of each version. To save time, it is better for you to use a smaller training epoches (like 5 epoches), or you can impelement early stopping mechanism (like if the loss on val dataset keeps rising in the consecutive 3 rounds then stop training).
2. Running the demos again, especially weight analysis of SigmaSelector in urban and natural areas, and get the similar figures and plots which are used for paper writing.
3. Modify the paper and recompile it (result data tables, remove limitations and so on).
4. Modify docs and README.md based on your modification if necessary.

## Notice
You can run any no-sudo commands without asking me for permission.
